---
title: "Kappa"
summary:
  "How the kappa subnet hyperparameter sets the consensus majority ratio that Yuma Consensus uses to
  decide which validator weights count."
category: "Consensus"
tags: ["Consensus", "Subnets"]
infoboxTitle: Kappa
infoboxCaption: Consensus majority ratio for a subnet.
infoboxRows:
  - label: Context
    value: Bittensor subnets
  - label: Example
    value: netuid 1
  - label: Setting
    value: Root-level
  - label: Scope
    value: Concept only
---

# Kappa

Kappa is a per-subnet hyperparameter that sets the consensus majority ratio used by Yuma Consensus.
Documentation describes it as the consensus majority ratio and states that the weights set by
validators with lower normalized stake than kappa are not used in calculating consensus
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## What It Controls

Kappa marks the stake-weighted majority point used when validator weights are combined into a
consensus result. Per the documentation, weights coming from validators whose normalized stake falls
below kappa do not enter the consensus calculation. In effect, kappa sets how much of the
stake-weighted agreement forms consensus and where lower-stake input stops counting
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Subnet Hyperparameters: Kappa](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#kappa)).

## Default and Representation

The documentation lists kappa as a `u16` value with a default of 32767, which is approximately 0.5
once normalized against the maximum of that type. That places the default near the halfway point of
stake-weighted agreement. The value actually in force is per-subnet chain state and can differ from
the documented default
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Subnet Context

Kappa is one of the subnet hyperparameters, the on-chain state variables that configure a single
subnet, for example netuid 1. Unlike hyperparameters a subnet owner can change directly, the
documentation marks kappa as a root-level parameter, so it is governed at that level rather than set
by ordinary participants
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Subnet Hyperparameters, Consensus Clipping, and Yuma Weight Pipeline

Yuma Consensus aggregates validator weights into a stake-weighted result, and kappa is the majority
ratio that aggregation relies on to separate the broad stake-weighted agreement from lower-stake
input. This makes kappa a parameter behind the consensus threshold rather than a value assigned to
any single validator or miner
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus) documentation describes
consensus clipping as trimming validator weights that depart from the stake-weighted agreement
point. Kappa names which stake-weighted validator input counts toward that agreement, while clipping
names how far individual weight values can depart before they stop being rewarded.

Those are sequential defenses inside the same epoch. Kappa filters which validator rows participate
in the majority calculation on a subnet such as netuid 1, and clipping then punishes inflated
ratings that still outlie the resulting agreement
([Understanding Incentive Mechanisms](https://docs.learnbittensor.org/learn/anatomy-of-incentive-mechanism)).

Official documentation puts the mechanism in concrete terms: if the less generous half of
validators, measured by stake rather than by validator count, set a given miner's weight to no more
than some value, the more generous half's weights for that same miner are clipped down to that value
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

Kappa's stake-ranking scale, not a raw neuron count, is what decides which validator rows clear that
threshold. A validator below the majority ratio stops contributing weight rows to the consensus
calculation even if it still holds a
[validator permit](https://docs.learnbittensor.org/resources/glossary#validator-permit) on the
subnet ([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

After kappa filters validator input and clipping trims outlying weights, Yuma Consensus continues
through bonding and emission calculation to produce miner incentives and validator dividends
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)). Kappa sits at the front of
that weight pipeline; it does not name the bond or dividend outcomes that follow.

## Development Stage Context

The
[Introduction to Bittensor](https://docs.learnbittensor.org/learn/introduction#subnet-development)
describes subnet development as moving from localnet to testnet and then mainnet. For kappa on a
subnet such as netuid 1, that sequence changes how the consensus majority ratio in force should be
read, because the value is per-subnet chain state rather than a fixed constant.

In localnet, kappa can be exercised in an isolated environment to observe how the majority ratio
filters lower-stake validator weights on netuid 1. A localnet kappa value reflects that test chain
state rather than production governance.

On testnet, kappa applies on a shared non-production network where validator stake and weights on
netuid 1 are separate from mainnet. A testnet kappa reading describes that environment's chain
state.

On mainnet, kappa is the root-level consensus majority ratio governing live subnets such as netuid
1, where the value in force drives real Yuma Consensus outcomes
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

The [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks) reference
separates mainnet, testnet, and localnet. A kappa value observed in one environment should not be
read as the parameter in force on another.

## Kappa Filters by Stake, Not by Validator Headcount

A point that is easy to misread is that kappa is a stake-weighted threshold, not a count of
validators. Documentation describes it as the consensus majority ratio measured against normalized
stake, so a small number of high-stake validators can clear it while many low-stake validators do
not ([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

For readers, this is why kappa is described in terms of "the less generous half by stake" rather
than by number. The majority that forms consensus is weighed by how much stake stands behind each
validator's weights, which is different from a one-vote-per-validator tally
([Understanding Incentive Mechanisms](https://docs.learnbittensor.org/learn/anatomy-of-incentive-mechanism)).

That stake-based framing also clarifies why holding a permit is not enough to count. A validator can
hold a permit yet still fall below the stake-weighted majority point, in which case its weights do
not enter the consensus calculation for that epoch
([Glossary: Validator Permit](https://docs.learnbittensor.org/resources/glossary#validator-permit)).

## Reader Boundary

This page defines the concept at a high level. It does not report the kappa value set on any
particular subnet or the current consensus outcome of an epoch. Those are live chain state and
should be checked for the relevant netuid. The 32767 figure, about 0.5 normalized, is the documented
default, and the parameter is governed at the root level
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Relationship to Consensus Score

Kappa sets the stake-weighted majority ratio that decides which validator weights enter Yuma
Consensus aggregation. Consensus score is the stake-weighted median benchmark used when clipping
outlier weights during that process
([Subnet Hyperparameters: Kappa](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#kappa),
[Glossary: Consensus Score](https://docs.learnbittensor.org/resources/glossary#consensus-score),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

Readers comparing threshold rules need both terms on the same netuid. Kappa filters which validator
stake counts toward the majority calculation, while consensus score names the median benchmark that
clipping compares submitted weights against
([Yuma Consensus: Clipping](https://docs.learnbittensor.org/learn/yuma-consensus#clipping),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

Use kappa when covering the majority-ratio hyperparameter; use consensus score when covering the
median clipping benchmark rather than the stake threshold itself
([Understanding Incentive Mechanisms](https://docs.learnbittensor.org/learn/anatomy-of-incentive-mechanism),
[Glossary: Weight Vector](https://docs.learnbittensor.org/resources/glossary#weight-vector)).

Majority threshold and clipping benchmark answer different reader questions inside the same epoch
([Glossary: Trust](https://docs.learnbittensor.org/resources/glossary#trust)).

## Relationship to Bonds Penalty

Kappa and bonds penalty are distinct Yuma Consensus hyperparameters. Kappa sets how much
stake-weighted validator support a weight level must reach before it becomes the clipping ceiling
during Yuma Consensus, while bonds penalty sets the magnitude of the penalty applied when validator
weights exceed that consensus benchmark
([Subnet Hyperparameters: Kappa](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#kappa),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

- **Kappa** — the stake fraction that sets the consensus clipping ceiling.
- **Bonds penalty** — the penalty magnitude applied to weights that exceed that benchmark.

## Further Reading

- [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
- [Subnet Hyperparameters: Kappa](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#kappa)
- [Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)
- [Understanding Incentive Mechanisms](https://docs.learnbittensor.org/learn/anatomy-of-incentive-mechanism)
- [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)
