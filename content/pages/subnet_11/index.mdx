---
title: "Subnet 11: TrajectoryRL"
summary:
  "TrajectoryRL is Bittensor Subnet 11. Its on-chain identity describes the subnet as agentic RL as
  a service, and its official repository presents it as a continuous competition for producing
  installable AI agent skills."
category: "Subnets"
tags: ["Subnets"]
infoboxTitle: Subnet 11 — TrajectoryRL
infoboxCaption: Agentic RL as a service on Bittensor.
infoboxRows:
  - label: Subnet
    value: SN11
  - label: Neurons
    value: "256"
  - label: GitHub
    value: trajectoryRL/trajectoryRL
  - label: Site
    value: trajrl.com
---

# Subnet 11: TrajectoryRL

TrajectoryRL is Bittensor Subnet 11. [TaoStats](https://taostats.io/subnets/11) describes the live
on-chain identity as "Agentic RL as a Service, Optimize agent trajectories to make agents cheaper,
safer, and more reliable." The subnet's official
[trajectoryRL/trajectoryRL](https://github.com/trajectoryRL/trajectoryRL) repository presents
TrajectoryRL as a continuous reinforcement learning competition in which miners produce AI agent
skills and validators evaluate them in sandboxed scenarios.

## What TrajectoryRL Provides

According to the [official repository](https://github.com/trajectoryRL/trajectoryRL), TrajectoryRL
is organized around installable `SKILL.md` files for AI agents. The README says miners compete to
produce the best skills, validators test those skills in real sandboxes, and the winning skills
surface through the subnet's associated tooling.

The same source describes the subnet as a continuous competition rather than a one-time benchmark.
Its stated aim is to improve the quality and reliability of agent skills over repeated evaluation
cycles.

## Skill Artifact Context

The [TrajectoryRL repository](https://github.com/trajectoryRL/trajectoryRL) describes the miner
artifact as a `SKILL.md` pack rather than a model endpoint or always-on server. For a reader, that
changes how to interpret the subnet's output: the durable product is an instruction scaffold that
can be installed by an agent and reused across tasks, not just a single benchmark answer. That
separates miner work from validator infrastructure: miners compete on reusable agent guidance, while
validators measure whether that guidance improves task completion.

The [miner guide](https://github.com/trajectoryRL/trajectoryRL/blob/main/docs/MINER_GUIDE.md)
supports that reading by describing `SKILL.md` as the persistent context an agent receives before
working through scenarios. Competitive miners therefore improve the way an agent approaches classes
of operational problems. A skill that only memorizes a known scenario is weak, because it does not
help the agent generalize when the next task changes.

References: [TrajectoryRL repository](https://github.com/trajectoryRL/trajectoryRL),
[TrajectoryRL miner guide](https://github.com/trajectoryRL/trajectoryRL/blob/main/docs/MINER_GUIDE.md)

## Sandbox Scoring Context

The [trajrl-bench repository](https://github.com/trajectoryRL/trajrl-bench) describes the benchmark
as scenario containers with programmatic verifiers. Validators run the agent through each scenario,
then score the produced output against tests. That makes SN11 closer to a software-task evaluation
market than to a subjective prompt-ranking contest.

This context explains why the article emphasizes sandboxed scenarios. The useful signal is whether
the submitted skill causes the agent to complete concrete tasks, produce the requested deliverable,
and pass verifier checks across the active scenario set. The scoring source describes quality as the
passed-test share per scenario, summed across scenarios, so breadth and reliability both matter.

References: [trajrl-bench](https://github.com/trajectoryRL/trajrl-bench),
[TrajectoryRL miner guide](https://github.com/trajectoryRL/trajectoryRL/blob/main/docs/MINER_GUIDE.md)

## Miner and Validator Roles

TrajectoryRL follows the standard Bittensor subnet structure. Miners submit work to the subnet, and
validators evaluate miner performance and set weights. Those validator assessments feed into
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus), which determines how subnet
emissions are distributed.

The [official repository](https://github.com/trajectoryRL/trajectoryRL) says TrajectoryRL's miner
role centers on producing agent skills, while the validator role centers on evaluating those skills
in sandboxed scenarios.

## On-Chain Identity

The on-chain identity for netuid 11 lists the following public metadata:

- **Subnet name:** TrajectoryRL
- **Description:** Agentic RL as a Service, Optimize agent trajectories to make agents cheaper,
  safer, and more reliable.
- **GitHub repository:** [trajectoryRL/trajectoryRL](https://github.com/trajectoryRL/trajectoryRL)
- **Project URL:** [trajrl.com](https://trajrl.com)
- **Discord:** The on-chain Discord field is blank.
- **Owner coldkey:** `5D2Jhtbnm7iAdKfjRk6DisXBnr1MEsYat8kXqaPNrVqJP3uE`
- **Neurons:** 256

Live identity data is available on [TaoStats](https://taostats.io/subnets/11).

## Development Stage Context

The
[Introduction to Bittensor](https://docs.learnbittensor.org/learn/introduction#subnet-development)
describes subnet development as moving from localnet to testnet and then mainnet. For TrajectoryRL
(SN11), that sequence changes how readers should interpret skill evaluation examples and sandbox
scoring outcomes.

In localnet, TrajectoryRL-compatible agents and skills can be developed and evaluated in an isolated
environment. Sandbox skill scores and emission outcomes from localnet do not represent production
subnet performance.

On testnet, agent skills can be exercised in a shared, non-production network. Testnet skill
evaluations and validator scores are separate from mainnet subnet state.

On mainnet, TrajectoryRL (SN11) is the live production subnet where validators score miner skills
against sandboxed scenarios to determine real Bittensor emissions. The
[TrajectoryRL repository](https://github.com/trajectoryRL/trajectoryRL) describes the mechanism that
applies on the production network.

The [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks) reference
separates mainnet, testnet, and localnet. A skill evaluation score or emission example from one
environment should not be read as representing production subnet performance in another environment.

## SKILL.md Artifacts Are the Miner Deliverable

The TrajectoryRL repository describes the miner artifact as an installable `SKILL.md` pack rather
than a model endpoint or always-on server
([TrajectoryRL repository](https://github.com/trajectoryRL/trajectoryRL)).

Readers should evaluate miner output as reusable agent guidance rather than as a single benchmark
answer.

## Sandbox Verifiers Define the Scored Signal

The trajrl-bench repository describes scenario containers with programmatic verifiers. Validators
run agents through scenarios and score passed-test results across the active scenario set
([trajrl-bench](https://github.com/trajectoryRL/trajrl-bench)).

That makes the useful signal task completion under verifiers rather than subjective prompt ranking.

## Validator Weights Still Flow Through Yuma Consensus

Subnet 11 uses Yuma Consensus to convert validator weight submissions into emission shares each
tempo ([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

## Distinction from Multiple Incentive Mechanisms

TrajectoryRL has validators evaluate miner agent skills in sandboxed scenarios before setting
weights. The
[Glossary: Multiple Incentive Mechanisms](https://docs.learnbittensor.org/resources/glossary#multiple-incentive-mechanisms)
and
[Multiple Incentive Mechanisms Within Subnets](https://docs.learnbittensor.org/subnets/understanding-multiple-mech-subnets)
docs note that validators must evaluate miners separately for each mechanism when a netuid runs more
than one.

For readers, this article documents one subnet market. If netuid 11 runs more than one incentive
mechanism, validator scores and weights should be read per mechanism rather than as one combined
path.

## Distinction from Yuma Consensus

TrajectoryRL on netuid 11 is an agentic reinforcement-learning subnet where miners submit
installable SKILL.md agent packs and validators score them in sandboxed scenarios with programmatic
verifiers. That skill-evaluation vocabulary names how TrajectoryRL measures agent guidance quality
on netuid 11, not the on-chain step that turns validator weight submissions into emission shares
each tempo ([TrajectoryRL repository](https://github.com/trajectoryRL/trajectoryRL),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

Yuma Consensus is the on-chain mechanism that runs at the epoch boundary on the selected netuid. It
reads the weight matrix from eligible validators, applies clipping and bonding, and converts the
result into miner incentives and validator dividend shares
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

Earlier sections separate reusable skill artifacts from sandbox scoring. Validators run agents
through scenario containers and score passed-test shares across the active scenario set; miners
compete on generalizable agent guidance rather than one benchmark answer. Yuma aggregates those
submitted weight signals ([trajrl-bench](https://github.com/trajectoryRL/trajrl-bench),
[Understanding Subnets](https://docs.learnbittensor.org/subnets/understanding-subnets)).

Programmatic verifiers therefore define the upstream evaluation signal. Yuma still settles Bittensor
emissions from included validator weights on netuid 11 once the tempo closes
([TrajectoryRL miner guide](https://github.com/trajectoryRL/trajectoryRL/blob/main/docs/MINER_GUIDE.md),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

Readers should treat TrajectoryRL as the sandboxed agent-skill protocol on netuid 11, and Yuma
Consensus as the recurring mechanism that allocates rewards from validator weights each tempo.

- **Subnet 11 (TrajectoryRL)** — SKILL.md agent-skill subnet with programmatic verifier scoring.
- **Yuma Consensus** — epoch-boundary settlement from validator weights to emission shares on a task
  subnet
  ([Yuma Consensus: Validator emissions](https://docs.learnbittensor.org/learn/yuma-consensus#validator-emissions)).

## Reader Boundary

Subnet 11 TrajectoryRL should not be read as generic Bittensor subnet documentation, a guarantee of
agent safety outcomes, or proof that every skill format scores the same way. It names one subnet's
agentic reinforcement-learning competition on netuid 11
([Understanding Subnets](https://docs.learnbittensor.org/subnets/understanding-subnets),
[Glossary: Netuid](https://docs.learnbittensor.org/resources/glossary#netuid)).

## Further Reading

- [Subnet 11 on TaoStats](https://taostats.io/subnets/11)
- [TrajectoryRL GitHub repository](https://github.com/trajectoryRL/trajectoryRL)
- [trajrl.com](https://trajrl.com) — official site
- [Introduction to Bittensor: Subnet development](https://docs.learnbittensor.org/learn/introduction#subnet-development)
- [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)
- [Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus) — the weight-setting
  protocol used across all subnets
- [Emission](https://docs.learnbittensor.org/learn/emissions)
