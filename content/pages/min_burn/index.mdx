---
title: "Min Burn"
summary:
  "How the min burn subnet hyperparameter sets the floor on the dynamic TAO burn required to
  register a neuron on a subnet."
category: "Subnets"
tags: ["Subnets", "Registration"]
infoboxTitle: Min Burn
infoboxCaption: Floor on the registration burn.
infoboxRows:
  - label: Context
    value: Bittensor subnets
  - label: Example
    value: netuid 1
  - label: Setting
    value: Owner-settable
  - label: Scope
    value: Concept only
---

# Min Burn

Min burn is a per-subnet hyperparameter that sets the lower bound on the dynamic TAO burn required
to register a neuron on a subnet. Official documentation describes it as the lower bound for that
registration burn, gives a default of 0.0005 TAO, and marks it owner-settable
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## What It Controls

Registering a neuron burns TAO, and that cost is dynamic: it rises as registrations occur and eases
back during quieter periods, moving between a lower and an upper bound. Min burn fixes the bottom of
that range, so the registration cost can settle toward it but not below it. It does not set the
current cost or decide who registers; it only limits how low the burn can fall
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

The actual cost a registrant pays at any block depends on recent registration activity and on how
`BurnHalfLife`, `BurnIncreaseMult`, and the burn bounds interact for that subnet
([Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

## Default and Setting

The documentation lists a default of 0.0005 TAO and marks the parameter as owner-settable, so a
subnet owner can adjust the floor for their subnet. The value in force on netuid 1 can differ from
the documented default and from another subnet's floor
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Distinction from BurnHalfLife

`BurnHalfLife` names how quickly the live registration burn decays toward the subnet floor when
demand quiets down. Min burn names the floor that decay approaches but cannot pass
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#burnhalflife),
[Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

- **Min burn** — floor the registration burn cannot fall below.
- **BurnHalfLife** — decay rate pulling the live burn down during quiet periods.

## Distinction from BurnIncreaseMult

`BurnIncreaseMult` steps the live registration burn upward after each successful registration inside
the documented min/max band. Min burn sets where that upward stepping begins, not how far it can
climb
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#burnincreasemult),
[Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

- **Min burn** — floor the registration burn cannot fall below.
- **BurnIncreaseMult** — per-registration multiplier that climbs toward max burn.

## Distinction from Max Burn

Min burn and max burn set the lower and upper bounds of the same dynamic registration burn band on a
subnet. Registration cost moves between those limits as demand changes
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Mining: Miner registration](https://docs.learnbittensor.org/miners#miner-registration)).

- **Min burn** — floor the registration burn cannot fall below.
- **Max burn** — ceiling the registration burn cannot exceed.

## Distinction from Registration Allowed

Registration allowed controls whether new neurons can register on a subnet at all. Min burn answers
how low registration cost can fall when entry is open, not whether registration is currently
permitted ([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Register](https://docs.learnbittensor.org/resources/glossary#register)).

- **Registration allowed** — whether new entry is currently open.
- **Min burn** — floor on dynamic registration burn when entry is open.

## Distinction from Burn Cost

Burn cost names the live registration charge at a moment in time. Min burn names the configured
floor that charge cannot fall below
([Glossary: Burn Cost](https://docs.learnbittensor.org/resources/glossary#burn-cost),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

- **Burn cost** — observed registration charge at a point in time.
- **Min burn** — configured floor that charge cannot fall below.

## Distinction from Subnet-Creation Burn Cost

Min burn bounds the dynamic neuron registration burn on an existing subnet. Subnet-creation burn
cost is the separate recycled TAO amount required to open a new subnet market
([Subnet Hyperparameters: MinBurn](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#minburn),
[Create a Subnet: Burn cost](https://docs.learnbittensor.org/subnets/create-a-subnet#burn-cost)).

- **Min burn** — floor on dynamic neuron registration burn on an existing subnet.
- **Subnet-creation burn cost** — recycled TAO required to create a new subnet market.

## Distinction from Existential Deposit

Existential deposit is the minimum TAO balance required to keep a Bittensor account represented on
chain. Min burn is a subnet hyperparameter floor on the registration burn paid to obtain a UID slot
([Glossary: Existential Deposit](https://docs.learnbittensor.org/resources/glossary#existential-deposit),
[Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

- **Existential deposit** — minimum wallet balance to keep an account on chain.
- **Min burn** — subnet hyperparameter floor on the registration burn itself.

## Chain Reads for netuid 1

Readers can verify live hyperparameter values for the documented example netuid with
`btcli subnet hyperparameters --netuid 1 --network finney`
([Subnet Hyperparameters: View hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#view-hyperparameters)).

That read path keeps live hyperparameter claims tied to a parseable netuid.

## Per-Subnet Live Value Boundary

Min burn is per-subnet chain state. The registration-burn floor configured on one netuid sets how
low the dynamic charge can fall on that subnet, but the live floor can differ from reference
defaults and from the floor on another netuid
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

This article's infobox uses netuid 1 as an example label when reading one subnet's hyperparameter
table on Finney mainnet. That example helps readers verify one row; it is not proof that every
subnet exposes the same live floor
([Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)).

Min burn governs registration pricing floors; Yuma Consensus still settles validator weights into
emission shares each epoch on the active subnet
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

- **Min burn** — floor the dynamic registration burn cannot fall below on a subnet.
- **Per-subnet boundary** — live value is chain state on one netuid.

## Distinction from Yuma Consensus

Min burn is a per-subnet hyperparameter that sets the floor of the dynamic registration burn band.
Yuma Consensus is the separate tempo process that converts validator weight submissions into
emission shares each epoch
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

- **Min burn** — floor the registration burn cannot fall below.
- **Yuma Consensus** — on-chain settlement that turns validator weights into emission shares. Min
  burn names registration burn floors; Yuma processes validator weights into rewards
  ([Yuma Consensus: Validator emissions](https://docs.learnbittensor.org/learn/yuma-consensus#validator-emissions)).

- **Registration vs settlement** — min burn caps registration economics; Yuma aggregates validator
  weights into emission shares at epoch boundaries
  ([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
  [Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).
- **Floor not price** — min burn bounds the band; live burn still moves inside min/max limits
  ([Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

## Reader Boundary

This page defines the concept. It does not report the min burn value or the current registration
cost on any particular subnet; those are live chain state to check for the relevant netuid
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Further Reading

- [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
- [Register](https://docs.learnbittensor.org/resources/glossary#register)
- [Glossary: Burn Cost](https://docs.learnbittensor.org/resources/glossary#burn-cost)
- [Miner registration](https://docs.learnbittensor.org/miners#miner-registration)
