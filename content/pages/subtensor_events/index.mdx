---
title: "Subtensor Events"
summary: "How Subtensor runtime events provide named records of Bittensor chain activity."
category: "Core Concepts"
tags: ["Subtensor", "Events"]
infoboxTitle: Subtensor Events
infoboxCaption: Runtime event records in Subtensor.
infoboxRows:
  - label: Layer
    value: Subtensor runtime
  - label: Form
    value: Named events
  - label: Organized by
    value: Runtime area
  - label: Purpose
    value: Chain activity records
---

# Subtensor Events

Subtensor events are named records emitted by Bittensor's Subtensor runtime
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)).

They help readers see what the chain recorded during runtime activity, especially when inspecting
the result of a submitted action or following how a feature changed state.

## Event Records

An event shows that the runtime emitted a named record. For example, after an extrinsic is
submitted, related events can help show what the runtime recorded while processing that action
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics)).

The event name is useful because it gives the record a specific label. It still needs surrounding
context when the reader wants to understand why the action succeeded, failed, or changed state.

Event names are grouped by runtime area. The area matters because records under different runtime
areas can describe different parts of Subtensor activity
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage)).

The area and event name travel together. Reading the event name alone can miss which part of the
runtime emitted the record.

## Reference Entries

The generated event reference identifies events through the `api.events.<Pallet>.<EventName>` shape
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)). That path keeps the
runtime area and event name attached in the same label.

Each listed event combines a signature with a short summary. The signature shows the event name and
argument types, while the summary gives the reader-facing record description
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

That makes the reference useful as vocabulary before any particular block is inspected. A reader can
identify the event type, runtime area, and argument shape without treating the reference entry as an
observed occurrence.

The event reference names event types that the runtime can emit; an emitted event is the concrete
record found in a particular block or transaction context. The Subtensor event listing describes the
named event surface, while extrinsic and inspection references explain where a reader may encounter
records produced during chain activity
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics),
[Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)).

That keeps a reference entry from being read as proof that the event happened. The surrounding
block, extrinsic, and network context determine whether the event is part of observed chain history.
A listed signature and summary describe vocabulary from the runtime spec rather than proof that a
record appeared in a particular block, so observed chain history still needs block, extrinsic, and
network context attached to the emitted record
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)).

## Examples and Scope

The reference includes events outside the Bittensor-specific pallet as well as events under
`subtensorModule` ([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)). For
example, balance events describe account-balance records, while Subtensor-module events describe
network-specific activity such as subnet settings, stake movement, and weight-related records. The
same listing also includes general runtime areas such as `system`, `utility`, and
`transactionPayment`.

Some Subtensor events carry typed arguments. The generated listing includes examples such as
`AddStakeBurn`, `ActivityCutoffSet`, and `BurnHalfLifeSet`, with each entry showing the event
signature before the summary
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

Other examples are narrower setting records. Admin and Subtensor-module events can record changes to
subnet parameters, feature toggles, or timing values, with the event signature showing which typed
values are present ([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor Constants](https://docs.learnbittensor.org/subtensor-api/constants)).

These examples show why event names are useful but limited. They identify what the runtime emitted,
not the whole explanation of the related mechanism.

## Registration and Staking Confirmation Pipeline

The
[Subtensor Pallets and Extrinsics](https://docs.learnbittensor.org/navigating-subtensor/subtensor-extrinsics)
overview states that some submitted actions can trigger on-chain events when they succeed, such as
confirming a successful [registration](https://docs.learnbittensor.org/resources/glossary#register)
or recording an updated staking balance.

That pattern clarifies why event records often appear next to a known submitted action. The
extrinsic names the submission, while the emitted event names the runtime's success record rather
than duplicating the submission itself
([Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

Failure can involve error information instead of or alongside those success-oriented records
([Subtensor Error Codes](https://docs.learnbittensor.org/errors)). Confirmation events and rejection
signals therefore belong to related but different reading paths.

For readers tracing registration or staking on a subnet, the practical sequence is: name the
submitted extrinsic, inspect paired events for runtime confirmation, and use storage or error
surfaces when the outcome needs stronger evidence than the event label alone.

## Weight-Setting Event Evidence

[Weight setting](https://docs.learnbittensor.org/resources/glossary#weight-vector) names the
validator process of scoring miners inside a subnet. Validators submit weight extrinsics; the
runtime can emit events while those submissions are processed
([Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

Events therefore sit beside weight-setting vocabulary rather than replacing it. An event name can
show that the runtime recorded a weight-related action, while
[validator weights](https://docs.learnbittensor.org/resources/glossary#weight-vector) and consensus
outcomes still need the subnet and tempo context that produced them
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

A weight-setting investigation on a selected
[netuid](https://docs.learnbittensor.org/resources/glossary#netuid) typically moves from submitted
extrinsic to emitted event to later metagraph or storage inspection, not from event label alone to
consensus result.

## Development Stage Context

The
[Introduction to Bittensor](https://docs.learnbittensor.org/learn/introduction#subnet-development)
describes subnet development as moving from localnet to testnet and then mainnet. For Subtensor
events on a subnet such as netuid 1, that sequence changes how an example should be read, because
the surrounding network state differs at each stage.

In localnet, Subtensor events can be exercised in an isolated development environment. Local event
records reflect local configuration rather than production chain history on the selected netuid.

On testnet, Subtensor events can be observed in a shared, non-production network. Testnet event
records on a selected netuid are separate from mainnet chain history
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

On mainnet, Subtensor events apply on the live, production Bittensor network, where the surrounding
state is real and persistent for the connected subnet context.

The [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks) reference
separates mainnet, testnet, and localnet. A Subtensor events example from one environment should not
be read as representing another environment.

## Errors and Events Answer Different Questions

Error information can explain why a submitted action failed or was rejected, while events describe
records the runtime emitted during processing
([Subtensor Error Codes](https://docs.learnbittensor.org/errors),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

For readers, a rejection path and a success-oriented confirmation event belong to related but
different inspection steps rather than interchangeable labels.

## The Event Catalog Follows Runtime Spec

The [Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events) reference is generated
from the Subtensor runtime spec rather than maintained as a standalone prose list. Its grouped
entries describe the event types the connected runtime exposes at generation time.

Runtime upgrades can change that exposed surface by adding, renaming, or retiring event types.
Inspection guidance notes that runtime metadata must be refreshed after a Bittensor runtime upgrade
so connected tools still describe the live chain accurately
([Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)).

That generation link separates catalog vocabulary from block history. A listed event type names what
the runtime can emit, while an event observed in a particular block is evidence from that moment in
chain history on the connected
[Subtensor](https://docs.learnbittensor.org/resources/glossary#subtensor) network.

## Distinction from Subtensor Storage

A Subtensor event and a storage entry answer different questions. An event names a record emitted
while the runtime processes activity, while a storage entry names a state surface that can be read
afterward ([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage)).

- **Subtensor event** — a record emitted during runtime activity.
- **Subtensor storage** — a readable state surface after processing.

Events sit between submitted actions and readable state. Extrinsics name actions submitted to the
runtime, events name records emitted while the runtime processes activity, and storage exposes state
that can be inspected later
([Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage)).

The three surfaces often belong in the same investigation, but they answer different questions. An
extrinsic says what was submitted, an event says what the runtime recorded, and storage can show the
state surface after processing.

The order matters for interpretation. A submitted action can produce events during processing, and
later storage inspection can show the state surface that remains visible after that activity
([Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage)).

Errors are another neighboring surface. A failed or rejected action can involve error information,
while events describe emitted records from runtime activity
([Subtensor Error Codes](https://docs.learnbittensor.org/errors),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

## Distinction from Extrinsics

A Subtensor event and an extrinsic sit on the record side and the submission side. An extrinsic
names an action submitted to the runtime, while an event names what the runtime recorded while
processing that action ([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics)).

- **Extrinsic** — an action submitted to the runtime.
- **Subtensor event** — the record emitted while that action is processed.

The
[Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)
guide describes a block explorer view where readers browse recent blocks and open a block number to
see the extrinsics it contains and the events linked to those extrinsics.

That layout keeps emitted records attached to the submitted actions that produced them. A
[block](https://docs.learnbittensor.org/resources/glossary#block) supplies ordering in chain
history, the extrinsic names what was submitted, and the paired events show what the runtime
recorded while processing that activity.

The explorer path therefore supports observation rather than catalog lookup alone. A reader can move
from ordered chain activity to submitted action to emitted record without treating an event name as
an isolated label.

## Distinction from Chain Reads

A chain read and a Subtensor event differ in what they show. A chain read inspects current public
state at a surface such as storage, while an event is a record the runtime emitted after an action
was processed
([Inspecting the Chain](https://docs.learnbittensor.org/concepts/inspecting-the-chain),
[Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

- **Chain read** — inspection of current public state.
- **Subtensor event** — a record emitted after an action is processed.

## Distinction from Subtensor Constants

Subtensor events are runtime records emitted after successful extrinsic processing. Subtensor
constants expose protocol-wide fixed values that do not change block to block
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor Constants](https://docs.learnbittensor.org/subtensor-api/constants)).

- **Subtensor events** — historical records of what happened after inclusion.
- **Subtensor constants** — durable configuration values on the chain.

Events show outcomes; constants show fixed parameters. Both are read-only observation paths
([Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)).

## Distinction from Bittensor Logging Levels

Bittensor logging levels classify local diagnostic message severity. Subtensor events are runtime
records emitted after successful extrinsic processing on chain
([Logging](https://docs.learnbittensor.org/concepts/bt-logging-levels),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

- **Bittensor logging levels** — local severity labels in software output.
- **Subtensor events** — on-chain records of runtime activity after inclusion.

Log levels help triage local output; events confirm what the chain recorded
([Glossary: Block](https://docs.learnbittensor.org/resources/glossary#block)).

## Distinction from Yuma Consensus

Subtensor events are on-chain event records emitted by the Subtensor runtime when state changes
occur. They name what the chain logged after inclusion, not the recurring settlement step that
converts validator weight submissions into emission shares on one netuid
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Navigating Subtensor](https://docs.learnbittensor.org/navigating-subtensor)).

Yuma Consensus is the on-chain mechanism that runs at the epoch boundary on each subnet. It reads
the weight matrix from eligible validators, applies clipping and bonding, and converts the result
into miner incentives and validator dividend shares
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

Earlier sections separate subtensor events from block headers and from log levels. Yuma settlement
steps can surface as runtime events when recorded; the event log confirms what executed—not the
consensus algorithm definition itself
([Glossary: Block](https://docs.learnbittensor.org/resources/glossary#block)).

Readers should treat subtensor events as chain audit vocabulary, and Yuma as the epoch-boundary
mechanism that allocates subnet rewards from consensus while validators remain active.

- **Subtensor events** — on-chain runtime event records.
- **Yuma Consensus** — epoch-boundary settlement from eligible validator weights to emission shares.

## Distinction from Network

Emitted event records belong to the network and block context where they were produced. Mainnet,
testnet, and localnet are separate Bittensor environments, so an event observed in one environment
should not stand in for another network's chain history
([Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks),
[Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)).

The event type can be reusable vocabulary, while a concrete emitted record belongs to the chain
history that produced it.

Block context supplies the ordering side of that history. An event observed in a block belongs to
that point in chain history, while the same event type can remain available as reference vocabulary
elsewhere
([Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

## Reader Boundary

Subtensor events should not be read as proof of off-chain intent, a complete mechanism explanation,
or a substitute for storage inspection. They name runtime records that show what Subtensor emitted
while processing chain activity
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Glossary: Subtensor](https://docs.learnbittensor.org/resources/glossary#subtensor)).

When success, failure, or changed state matters, event vocabulary should be read alongside the
submitted extrinsic, related error information, and any relevant storage value
([Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics),
[Subtensor Error Codes](https://docs.learnbittensor.org/errors),
[Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage)).

## Further Reading

- [Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)
- [Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage)
- [Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics)
- [Subtensor Constants](https://docs.learnbittensor.org/subtensor-api/constants)
- [Subtensor Error Codes](https://docs.learnbittensor.org/errors)
- [Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)
- [Glossary: Block](https://docs.learnbittensor.org/resources/glossary#block)
- [Glossary: Subtensor](https://docs.learnbittensor.org/resources/glossary#subtensor)
- [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)
