---
title: "Alpha Halving"
summary:
  "How an alpha halving reduces a subnet's alpha-token emission rate when the subnet's alpha supply
  reaches a threshold, distinct from the network-scoped TAO halving."
category: "Tokenomics"
tags: ["Tokenomics", "Subnets"]
infoboxTitle: Alpha Halving
infoboxCaption: Subnet-scoped halving of alpha-token emissions.
infoboxRows:
  - label: Context
    value: Subnet tokenomics
  - label: Scope
    value: Per subnet
  - label: Reduces
    value: Alpha emission rate
  - label: Contrasted with
    value: TAO halving
  - label: Article scope
    value: Concept only
---

# Alpha Halving

An alpha halving is a subnet-scoped emission-rate reduction for a subnet's alpha token. It applies
halving terminology to subnet alpha supply rather than to network-wide TAO supply
([Halving Mechanisms](https://docs.learnbittensor.org/concepts/halving),
[Glossary: Alpha Tokens](https://docs.learnbittensor.org/resources/glossary#alpha-tokens)).

The concept belongs to Bittensor tokenomics because emissions include both TAO and subnet-specific
alpha-token flows ([Emission](https://docs.learnbittensor.org/learn/emissions)).

## Subnet Scope

Each subnet has its own alpha token, so alpha halving is read in relation to one subnet's
alpha-token supply. It should not be treated as a network-wide event affecting every subnet in the
same way ([Understanding Subnets](https://docs.learnbittensor.org/subnets/understanding-subnets),
[Glossary: Alpha Tokens](https://docs.learnbittensor.org/resources/glossary#alpha-tokens)).

That scope is the main difference from TAO halving. TAO halving is network-token supply language;
alpha halving is subnet-token supply language.

Because alpha tokens are subnet-specific, an alpha-halving example needs the subnet attached. The
same halving terminology can describe the supply schedule, but the affected asset is the selected
subnet's alpha token rather than TAO or another subnet's alpha token.

## Emission Reduction

A halving reduces the rate at which new tokens are emitted after a supply threshold is reached. For
alpha halving, the reduced emission is the alpha-token emission for the relevant subnet
([Halving Mechanisms](https://docs.learnbittensor.org/concepts/halving)).

For readers, the useful point is narrow: alpha halving slows future alpha creation for that subnet.
It does not describe a transfer action, validator evaluation, or reward-quality score.

The threshold framing also keeps the term tied to emission rate rather than to a one-time user
action. The event is about how quickly new alpha is produced after the threshold, not about moving
existing alpha between accounts
([Halving Mechanisms](https://docs.learnbittensor.org/concepts/halving)).

## Alpha Token Context

Alpha tokens are subnet-specific assets used inside subnet tokenomics. Alpha halving matters because
it changes the emission rate of that subnet-specific asset
([Glossary: Alpha Tokens](https://docs.learnbittensor.org/resources/glossary#alpha-tokens),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

This keeps the terminology separate: alpha token names the asset, while alpha halving names an
emission-rate reduction for that asset.

That distinction is useful in Dynamic TAO discussions because alpha tokens and TAO can appear
together in subnet tokenomics. Alpha halving concerns the alpha-token side of that setting; it does
not rename the network token or the TAO reserve side
([Glossary: Alpha Tokens](https://docs.learnbittensor.org/resources/glossary#alpha-tokens),
[Understanding Subnets](https://docs.learnbittensor.org/subnets/understanding-subnets)).

## TAO Halving Contrast

Alpha halving and TAO halving use the same broad supply-threshold idea, but they apply at different
scopes. One belongs to a subnet's alpha-token supply, and the other belongs to the network's TAO
supply ([Glossary: Halving](https://docs.learnbittensor.org/resources/glossary#halving),
[Halving Mechanisms](https://docs.learnbittensor.org/concepts/halving)).

Keeping that distinction avoids reading a subnet-level alpha event as a network-level TAO event. The
shared word is halving; the asset and scope are different.

## Emission and Incentive Context

Alpha halving changes an emission-rate input. It does not by itself explain miner evaluation,
validator rewards, or subnet incentive design
([Emission](https://docs.learnbittensor.org/learn/emissions),
[Understanding Incentive Mechanisms](https://docs.learnbittensor.org/learn/anatomy-of-incentive-mechanism)).

Those later outcomes still depend on the subnet's incentive mechanism and consensus context. Alpha
halving names the supply-rate change for the subnet alpha token.

That means alpha halving can change the emission setting without explaining how miners are evaluated
inside the subnet. Incentive mechanisms still define useful work, and consensus still organizes
validator evaluation
([Understanding Incentive Mechanisms](https://docs.learnbittensor.org/learn/anatomy-of-incentive-mechanism)).

## Reader Context

Alpha halving should be read as concept-level subnet tokenomics terminology
([Halving Mechanisms](https://docs.learnbittensor.org/concepts/halving)). The stable point is that
alpha-token emission can be reduced at the subnet level while remaining distinct from broader TAO
halving.

The clearest references name the subnet, the alpha token, and the halving context being discussed.
Timing examples and observed values need that same subnet context because alpha halving is not a
network-wide TAO event.

## Environment Context

The [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks) reference
separates mainnet, testnet, and localnet. Alpha halving keeps the same subnet-alpha meaning across
those environments, but concrete alpha-emission examples belong to the network environment and
subnet where they were measured.

The
[Introduction to Bittensor](https://docs.learnbittensor.org/learn/introduction#subnet-development)
places subnet development across localnet, testnet, and mainnet. Alpha-halving examples are easiest
to compare when the environment, subnet, and alpha-token context stay attached.

## Further Reading

- [Introduction to Bittensor: Subnet development](https://docs.learnbittensor.org/learn/introduction#subnet-development)
- [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)
- [Halving Mechanisms](https://docs.learnbittensor.org/concepts/halving)
- [Glossary: Halving](https://docs.learnbittensor.org/resources/glossary#halving)
- [Emission](https://docs.learnbittensor.org/learn/emissions)
- [Glossary: Alpha Tokens](https://docs.learnbittensor.org/resources/glossary#alpha-tokens)
- [Understanding Subnets](https://docs.learnbittensor.org/subnets/understanding-subnets)
- [Understanding Incentive Mechanisms](https://docs.learnbittensor.org/learn/anatomy-of-incentive-mechanism)
