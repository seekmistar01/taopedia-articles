---
title: "Toggle Transfer"
summary:
  "How the toggle transfer subnet hyperparameter allows or disallows transferring staked alpha
  between coldkeys on a subnet."
category: "Subnets"
tags: ["Subnets", "Staking"]
infoboxTitle: Toggle Transfer
infoboxCaption: Allows or disallows stake transfer between coldkeys.
infoboxRows:
  - label: Parameter
    value: ToggleTransfer
  - label: Type
    value: Boolean
  - label: Default
    value: "True"
  - label: Example
    value: netuid 1
  - label: Set by
    value: Subnet owner
---

# Toggle Transfer

Toggle transfer is a per-subnet hyperparameter, listed in the documentation as `ToggleTransfer`,
that allows or disallows transferring stake between
[coldkeys](https://docs.learnbittensor.org/resources/glossary#coldkey) on a subnet. The
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters) reference
lists it as the control that allows or disallows transfer of stake between coldkeys.

## What It Controls

The hyperparameter gates whether a subnet's staked alpha can move from one coldkey to another. When
it is enabled, holders can transfer their staked position between coldkeys; when it is disabled,
that transfer path is closed for the subnet
([Glossary: Stake](https://docs.learnbittensor.org/resources/glossary#stake)).

It governs only the coldkey-to-coldkey transfer permission. It does not set fees, define how staking
or unstaking works, or change how emissions are calculated; those belong to other parts of the
staking and consensus systems
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Documented Type, Default, and Setter

The [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
reference lists `ToggleTransfer` as a Boolean with a default of `True`, so stake transfer between
coldkeys is permitted by default. Because each subnet carries its own hyperparameters, the
permission is defined per subnet and applies only within that subnet.

The value in force is per-subnet chain state and can differ from the documented default, so whether
transfer is currently allowed should be read for the subnet in question rather than assumed.

## Distinction from Transferring Stake

Transferring stake is the action of moving a staked position between coldkeys, while toggle transfer
is the per-subnet switch that determines whether that action is permitted at all
([Glossary: Coldkey](https://docs.learnbittensor.org/resources/glossary#coldkey),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

- **Transferring stake** — the operation that moves stake to another coldkey.
- **Toggle transfer** — the subnet setting that allows or blocks that operation.

## Distinction from Moving Stake

Moving stake generally refers to relocating a staked position between hotkeys or subnets, while
toggle transfer specifically governs coldkey-to-coldkey transfer permission on a subnet. The toggle
is about who may hold the stake, not which hotkey or subnet it backs
([Glossary: Stake](https://docs.learnbittensor.org/resources/glossary#stake)).

- **Moving stake** — relocating stake across hotkeys or subnets.
- **Toggle transfer** — permitting or blocking coldkey-to-coldkey transfer.

## BTCLI Reads the Live Value

Any user can read subnet hyperparameters for a selected netuid with
`btcli subnet hyperparameters --netuid 1`. The output includes the live `ToggleTransfer` value for
that subnet at query time
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

Live values are network- and subnet-specific and can differ from the documented default, so a
transfer-permission claim should be checked against the hyperparameter table for the relevant netuid
rather than assumed from the reference default alone.

## Why a Transfer Switch Exists

A per-subnet transfer switch lets a subnet owner decide whether staked positions on their subnet can
change coldkey ownership. Leaving it at the default keeps transfer open, while disabling it
restricts how staked alpha can change hands within that subnet
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

For readers, toggle transfer is a permission boundary rather than an economic rate. It answers
whether a coldkey-to-coldkey transfer is allowed on the subnet, not what such a transfer would cost
or how it would affect emissions
([Glossary: Stake](https://docs.learnbittensor.org/resources/glossary#stake)).

## Further Reading

- [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
- [Glossary: Coldkey](https://docs.learnbittensor.org/resources/glossary#coldkey)
- [Glossary: Stake](https://docs.learnbittensor.org/resources/glossary#stake)
- [Glossary: Netuid](https://docs.learnbittensor.org/resources/glossary#netuid)
