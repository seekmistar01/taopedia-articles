---
title: "Root Claim Type"
summary:
  "How root claim type names the Keep or Swap setting for handling Root Subnet alpha dividends."
category: "Staking"
tags: ["Staking", "Emissions"]
infoboxTitle: Root Claim Type
infoboxCaption: Keep or Swap handling for Root Subnet dividends.
infoboxRows:
  - label: Applies to
    value: Root Subnet staking
  - label: Options
    value: Keep or Swap
  - label: Dividend input
    value: Subnet alpha
  - label: Boundary
    value: Not claim timing
---

# Root Claim Type

Root claim type is the Keep or Swap setting for alpha dividends earned through Root Subnet staking.
Keep leaves processed dividends as originating-subnet alpha, while Swap converts processed dividends
to TAO and restakes them on Root
([Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

The term belongs to dividend-handling vocabulary. It names the selected outcome for processed root
claims, so it sits beside Root Subnet staking and emissions context rather than replacing them.

## Keep and Swap Setting

Root claim type is the preference applied when accumulated root-claim dividends are processed. It
answers how the dividends are handled, while claim timing answers when accumulated dividends are
processed
([Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

This distinction keeps the setting separate from the claim event. A root claim processes accumulated
dividends; root claim type determines the Keep or Swap outcome used during that processing.

## Keep and Swap Outcomes

Keep is the alpha-retention outcome. Earned alpha remains on the subnet that generated the dividend.
Swap is the TAO-restaking outcome. Accumulated alpha dividends are converted to TAO and restaked on
Root ([Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

Both outcomes apply to dividend output. Keep preserves the originating subnet alpha form after
processing, while Swap changes the processed dividend output into TAO and places that TAO back into
the Root staking context
([Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

That makes the Keep/Swap contrast an output-handling contrast. It explains how processed dividends
are handled after they exist in the root-claim flow.

The distinction also keeps the setting focused on output handling. Root claim type explains the
processed dividend path, while other staking changes need their own root-staking context.

## Emission Scope

Root claim type sits near emissions vocabulary because the underlying subject is dividend handling.
Emissions supply the broader reward context, while root claim type decides whether processed
dividend output remains as alpha or becomes TAO restaked on Root
([Emission](https://docs.learnbittensor.org/learn/emissions),
[Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

Emission context explains why the output can be discussed as alpha, TAO, dividends, and staking
context in the same page. Root claim type stays narrower than the emission process itself: it
describes the selected handling of processed dividend output, not the full path by which emissions
are generated or distributed ([Emission](https://docs.learnbittensor.org/learn/emissions),
[Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

When exact amounts, timing, or conversion results matter, the relevant claim record and network
context supply that information.

## Development Stage Context

The
[Introduction to Bittensor](https://docs.learnbittensor.org/learn/introduction#subnet-development)
describes subnet development as moving from localnet to testnet and then mainnet. For root claim
type, that sequence changes how readers should interpret Keep or Swap settings and Root Subnet
dividend handling examples.

In localnet, root claim type can be tested in an isolated environment. Local claim settings reflect
local chain configuration rather than production Root Subnet dividend behavior.

On testnet, root claim type can be observed in a shared, non-production network. Testnet Keep or
Swap context is separate from mainnet root-staking state
([Root Claim Overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

On mainnet, root claim type describes live dividend-handling preferences for Root Subnet staking on
the production Bittensor network.

The [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks) reference
separates mainnet, testnet, and localnet. A root-claim-type example from one environment should not
be read as representing production dividend settings in another environment.

## Network Scope

Bittensor separates localnet, testnet, and mainnet environments. Root-claim-type examples from one
environment should not be treated as evidence for another because balances, dividend state, and
claim outcomes are environment-specific
([Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks),
[Introduction to Bittensor: Subnet development](https://docs.learnbittensor.org/learn/introduction#subnet-development)).

## Claim Type Is Separate From Claim Timing

Root claim type names the Keep or Swap preference applied when dividends are processed, not when
they become claimable or how often claims run. Automatic and manual claim paths both apply the
configured type once accumulated alpha dividends are ready
([Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

For readers, that keeps the setting distinct from the claim event itself. Root claim type answers
what form processed dividend output takes; claim timing and thresholds answer when processing
occurs. Changing the type does not by itself trigger a claim, and processing a claim does not change
the underlying root stake on the
[Root Subnet](https://docs.learnbittensor.org/resources/glossary#root-subnetsubnet-zero).

## Distinction from Root Claims

Root claim type and root claims name adjacent but different parts of Root Subnet dividend handling.
Root claims describe processing accumulated alpha dividends from Root Subnet staking, while root
claim type names the Keep or Swap preference applied when that processing happens
([Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

- **Root claims** — the act of processing accumulated Root Subnet dividends.
- **Root claim type** — the Keep or Swap handling choice for processed output.

Root-claim documentation describes Keep as leaving processed dividend output as alpha and Swap as
converting that output to TAO restaked on the Root Subnet
([Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

Root claim type names that handling choice rather than the full dividend-generation path.

## Distinction from Root Proportion

Root claim type and root proportion answer different tokenomics questions. Root proportion describes
how much of one subnet's dividend split is attributed to root-routed TAO, while root claim type
describes how processed root-claim dividends are handled after they exist
([Glossary: Root Proportion](https://docs.learnbittensor.org/resources/glossary#root-proportion),
[Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

- **Root proportion** — subnet-level split reading between Root TAO and subnet alpha.
- **Root claim type** — Keep or Swap outcome for processed root-claim dividends.

## Distinction from Stake Weight

Root claim type and stake weight name different parts of the staking-and-emissions picture. Root
claim type is the Keep or Swap preference for how processed Root Subnet alpha dividends are handled,
while stake weight is the computed total stake value that determines a validator's consensus power
and emissions in a subnet, combining alpha stake and TAO stake via the TAO weight parameter
([Glossary: Stake Weight](https://docs.learnbittensor.org/resources/glossary#stake-weight)).

- **Root claim type** — the Keep or Swap handling choice for processed dividend output.
- **Stake weight** — the computed total stake value that determines a validator's consensus power
  and emissions.

## Distinction from Validator Permit

Root claim type and a validator permit address different questions. Root claim type names the Keep
or Swap outcome applied when accumulated Root Subnet alpha dividends are processed, while a
validator permit is a boolean flag indicating whether a neuron has validation rights within a
subnet, awarded to the top neurons by stake weight and required for setting weights and
participating in consensus
([Glossary: Validator Permit](https://docs.learnbittensor.org/resources/glossary#validator-permit)).

- **Root claim type** — the Keep or Swap handling choice for processed dividend output.
- **Validator permit** — a flag granting validation rights, required for setting weights and joining
  consensus.

## Distinction from Yuma Consensus

Root claim type names the category of a root-network reward claim. That classification vocabulary
belongs to root staking and claim workflows, not to subnet weight aggregation on a selected netuid
([Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

Yuma Consensus converts validator weight submissions into miner incentives and validator dividends
inside subnet markets when the tempo boundary arrives
([Emission](https://docs.learnbittensor.org/learn/emissions)).

A root claim type labels how root-network rewards are categorized for stakers. It should not be read
as a weight-submission or subnet-settlement action
([Glossary: Root Subnet](https://docs.learnbittensor.org/resources/glossary#root-subnetsubnet-zero)).

- **Root claim type** — category label for a root-network reward claim.
- **Yuma Consensus** — on-chain settlement that turns validator weights into emission shares.

## Distinction from Root Staking

Root claim type belongs to Root Subnet staking. The Root Subnet, also called Subnet Zero, is the
special subnet where validators can register and TAO holders can stake in a subnet-agnostic way, and
a root staker is the TAO holder using that path
([Glossary: Root Subnet](https://docs.learnbittensor.org/resources/glossary#root-subnetsubnet-zero),
[Glossary: Root Subnet/Subnet Zero](https://docs.learnbittensor.org/resources/glossary#root-subnetsubnet-zero)).

This connects the setting to root staking without merging the terms. Root staker names who is
staking through the root path; root claim type names the selected dividend-handling outcome.

The setting is therefore part of root-claim interpretation. It tells readers how the processed
dividend output is intended to be handled, while the cited claim context explains the amount,
timing, and network state.

Root proportion is nearby vocabulary but answers a different question. Root proportion describes the
portion of a subnet's dividend context attributed to the Root side, while root claim type describes
how processed root-claim dividends are handled
([Glossary: Root Proportion](https://docs.learnbittensor.org/resources/glossary#root-proportion),
[Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

Root proportion helps interpret the dividend split. Root claim type names Keep or Swap handling for
processed dividends.

## Reader Boundary

Root claim type identifies the selected Keep or Swap handling for processed Root Subnet alpha
dividends
([Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)).

When the focus is the act of processing accumulated dividends, root claim is the broader term. When
the focus is how processed dividend output is handled, root claim type is the precise term.

## Further Reading

- [Root Claim overview](https://docs.learnbittensor.org/staking-and-delegation/root-claims)
- [Glossary: Root Subnet/Subnet Zero](https://docs.learnbittensor.org/resources/glossary#root-subnetsubnet-zero)
- [Glossary: Root Proportion](https://docs.learnbittensor.org/resources/glossary#root-proportion)
- [Glossary: Emission](https://docs.learnbittensor.org/resources/glossary#emission)
- [Emission](https://docs.learnbittensor.org/learn/emissions)
- [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)
