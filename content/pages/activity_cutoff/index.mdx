---
title: "Activity Cutoff"
summary:
  "How the activity cutoff subnet hyperparameter sets the block window in which a validator must
  submit weights to take part in a Yuma Consensus epoch."
category: "Consensus"
tags: ["Consensus", "Subnets"]
infoboxTitle: Activity Cutoff
infoboxCaption: Block window for validator participation.
infoboxRows:
  - label: Context
    value: Bittensor subnets
  - label: Example
    value: netuid 1
  - label: Setting
    value: Owner-settable
  - label: Scope
    value: Concept only
---

# Activity Cutoff

Activity cutoff is a per-subnet hyperparameter that defines, in blocks, the window a validator has
to submit weights during a Yuma Consensus epoch. Documentation describes it as the number of blocks
for stake to become inactive for the purpose of the epoch: a validator that does not submit weights
within the first activity-cutoff blocks of an epoch cannot participate until the next epoch
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## What It Controls

The hyperparameter ties consensus participation to timely weight submission. Within each epoch, a
validator must submit its weights inside the activity-cutoff window. If it misses that window, the
documentation states it is treated as inactive for the epoch and waits until the next epoch to take
part again. The cutoff is measured in blocks, not in wall-clock time
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Subnet Context

Activity cutoff is one of the subnet hyperparameters, the on-chain state variables that configure a
single subnet, for example netuid 1. Like other hyperparameters it is owner-settable, so a subnet
owner can change it for their subnet. The documentation lists a default value of 5000 blocks, but
the value in force is per-subnet chain state and can differ from the default
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Why It Matters

The cutoff keeps an epoch's consensus based on validators that are currently participating. By
requiring weights within a bounded block window, it prevents a validator that has gone quiet within
the epoch from carrying influence into that epoch's result. Activity cutoff therefore gates which
validator weight submissions enter the current Yuma Consensus pass
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Activity Cutoff Applies Inside Each Epoch

Subnet [hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters) measure
activity cutoff in blocks within each
[epoch](https://docs.learnbittensor.org/resources/glossary#epoch). Because an epoch spans the block
count set by the subnet's [tempo](https://docs.learnbittensor.org/resources/glossary#tempo)
hyperparameter, the cutoff is an early-window rule inside one tempo-sized consensus period rather
than a separate schedule.

Documentation states that a
[validator](https://docs.learnbittensor.org/resources/glossary#subnet-validator) must submit
validator weights within the first activity-cutoff blocks of the epoch to participate in the current
pass ([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Glossary: Epoch](https://docs.learnbittensor.org/resources/glossary#epoch)).

## Missed Submission Waits for the Next Epoch

When a validator does not submit weights inside the activity-cutoff window, documentation treats
that validator as inactive for the current epoch and requires waiting until the next epoch begins
before participating again. The rule is about timely weight submission, not about removing stake
from the validator
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

That waiting period keeps one epoch's Yuma Consensus pass based on validators who submitted inside
the documented window on the selected subnet.

## Tempo Sets the Epoch Length the Cutoff Sits Within

The [Glossary: Tempo](https://docs.learnbittensor.org/resources/glossary#tempo) defines tempo as the
subnet-specific block interval over which Yuma Consensus calculates
[emissions](https://docs.learnbittensor.org/resources/glossary#emission) from the latest ranking
weight matrix. Activity cutoff therefore applies during the opening block window of each epoch
inside that tempo cadence.

Readers comparing the two hyperparameters should keep units straight. Tempo names how many blocks
group an epoch; activity cutoff names how many blocks at the start of that epoch allow timely weight
submission
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Relationship to Validator Permit

Activity cutoff and the validator permit are two different gates on whether a validator's weights
count in a consensus pass. Activity cutoff is the early-epoch block window that sets when weights
must arrive to be included, while the validator permit is the permission flag that records whether a
UID may submit validator weights at all
([Subnet Hyperparameters: ActivityCutoff](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#activitycutoff),
[Glossary: Validator Permit](https://docs.learnbittensor.org/resources/glossary#validator-permit)).

The two gates are checked separately. A permitted validator still has to submit inside the window,
so weights from a validator that holds the permit but arrives after the activity-cutoff window can
miss the current consensus pass
([Implementation of the Yuma Consensus Epoch](https://docs.learnbittensor.org/navigating-subtensor/epoch)).

## Relationship to Last Update

Activity cutoff and last update are read as a pair. Activity cutoff sets how many blocks a neuron
may go without updating, while last update is the block timestamp from which that window is measured
([Subnet Hyperparameters: ActivityCutoff](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#activitycutoff),
[Subnet Metagraph](https://docs.learnbittensor.org/subnets/metagraph)).

On its own neither value decides activity. Activity cutoff supplies the "how long" and last update
supplies the "when," so a neuron counts as inactive only when the gap since its last update exceeds
the cutoff
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Relationship to Weight Setting

Activity cutoff is the early-epoch block window in which a validator must submit weights to
participate in the current consensus pass. Weight setting is the validator action of assigning
scores to miners in those submissions
([Implementation of the Yuma Consensus Epoch](https://docs.learnbittensor.org/navigating-subtensor/epoch)).

Readers comparing participation timing to evaluation action need both terms. Activity cutoff names
when weights must arrive to count in the epoch, while weight setting names the scoring step that
produces those weights
([Glossary: Subnet Validator](https://docs.learnbittensor.org/resources/glossary#subnet-validator),
[Understanding Incentive Mechanisms](https://docs.learnbittensor.org/learn/anatomy-of-incentive-mechanism)).

Use activity cutoff when covering the submission window inside an epoch; use weight setting when
covering how validators score miners rather than the timing gate itself
([Glossary: Consensus Score](https://docs.learnbittensor.org/resources/glossary#consensus-score)).

## Development Stage Context

The
[Introduction to Bittensor](https://docs.learnbittensor.org/learn/introduction#subnet-development)
describes subnet development as moving from localnet to testnet and then mainnet. For activity
cutoff, that sequence changes how readers should interpret hyperparameter examples and validator
participation windows
([Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)).

In localnet, activity cutoff can be exercised on an isolated subnet without production stake
context. Localnet block counts and epoch timing do not represent mainnet validator participation
behavior.

On testnet, activity cutoff applies in a shared non-production network. Testnet epoch windows and
weight-submission outcomes are separate from mainnet subnet state.

On mainnet, activity cutoff is a live per-subnet hyperparameter on the production Bittensor network.
Observed cutoff values and validator participation depend on the selected subnet's on-chain
hyperparameter state
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

An activity-cutoff example from one environment should not be read as representing production
participation rules in another environment.

## Three Submission Gates Sit on the Same Weight Path

Official epoch documentation describes validator influence as passing through separate checks before
weights affect Yuma Consensus. Validator permit vocabulary decides whether a neuron may submit
non-self weights at all. Activity cutoff vocabulary decides whether that submission arrived inside
the opening block window of the current epoch. Version-key hyperparameters can reject submissions
whose declared client version is below the subnet floor
([Glossary: Validator Permit](https://docs.learnbittensor.org/resources/glossary#validator-permit),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Implementation of the Yuma Consensus Epoch](https://docs.learnbittensor.org/navigating-subtensor/epoch)).

For readers, those gates answer different failure modes on the same extrinsic boundary. Missing the
permit is an authorization failure. Missing the activity window is a timing failure inside the
tempo-sized epoch. Carrying a stale version is a compatibility failure. A validator can satisfy one
gate while failing another on the same round
([Glossary: Weight Vector](https://docs.learnbittensor.org/resources/glossary#weight-vector),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

That ordering keeps role vocabulary, hyperparameter timing, and client-version rollout as separate
ideas rather than one blended active-validator label
([Subnet Metagraph](https://docs.learnbittensor.org/subnets/metagraph)).

## Activity Cutoff Is Measured in Blocks, Not Wall-Clock Time

Subnet hyperparameter documentation describes activity cutoff as a block count inside each epoch.
Because epoch length is set by tempo on the same subnet, the wall-clock duration of the submission
window changes with network block production rather than with a fixed minute timer
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Glossary: Tempo](https://docs.learnbittensor.org/resources/glossary#tempo)).

For readers, activity cutoff vocabulary always pairs with block height and metagraph last-update
fields. Comparing schedules across subnets requires reading each subnet's tempo and cutoff together
instead of assuming identical real-time windows
([Subnet Metagraph](https://docs.learnbittensor.org/subnets/metagraph),
[Glossary: Epoch](https://docs.learnbittensor.org/resources/glossary#epoch)).

Missed submissions wait for the next epoch rather than retroactively joining the current pass
([Implementation of the Yuma Consensus Epoch](https://docs.learnbittensor.org/navigating-subtensor/epoch)).

## Distinction from Weights Rate Limit

Activity cutoff and weights rate limit answer different timing questions. Activity cutoff is
measured in blocks within each epoch and governs whether a validator's weight submission arrives
early enough to count for the current Yuma Consensus pass, while weights rate limit is measured in
blocks between commits and governs how soon the same validator can submit again after a prior weight
commit ([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

- **Activity cutoff** — whether a submission arrives inside the current epoch window.
- **Weights rate limit** — how long a validator must wait before the next commit.

## Reader Boundary

This page defines the concept at a high level. It does not report the activity-cutoff value set on
any particular subnet, current epoch timing, or which validators are active right now. Those are
live chain state that should be checked for the relevant netuid. The 5000-block figure here is the
documented default, and the value for a given subnet is owner-settable
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Further Reading

- [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
- [Glossary: Tempo](https://docs.learnbittensor.org/resources/glossary#tempo)
- [Glossary: Epoch](https://docs.learnbittensor.org/resources/glossary#epoch)
- [Implementation of the Yuma Consensus Epoch](https://docs.learnbittensor.org/navigating-subtensor/epoch)
- [Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)
- [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)
