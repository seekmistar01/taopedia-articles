---
title: "Max Burn"
summary:
  "How the max burn subnet hyperparameter caps the dynamic TAO burn required to register a neuron on
  a subnet."
category: "Subnets"
tags: ["Subnets", "Registration"]
infoboxTitle: Max Burn
infoboxCaption: Ceiling on the registration burn.
infoboxRows:
  - label: Context
    value: Bittensor subnets
  - label: Example
    value: netuid 1
  - label: Setting
    value: Owner-settable
  - label: Scope
    value: Concept only
---

# Max Burn

Max burn is a per-subnet hyperparameter that sets the upper bound on the dynamic TAO burn required
to register a neuron on a subnet. Documentation describes it as the upper bound for the dynamic TAO
burn required for neuron registration on the subnet, so it is the ceiling the registration cost
cannot exceed.

## What It Controls

The hyperparameter caps the registration burn rather than fixing it. Registering a neuron requires
burning an amount of TAO, and max burn is the most that amount can be on the subnet. It does not set
the current cost or decide who registers; it only limits how high the burn can climb.

## A Dynamic Cost Within Bounds

The registration burn is dynamic. The documentation describes it as moving between a lower bound and
this upper bound: the cost rises as registrations occur and eases back during quieter periods, while
staying inside that band. Max burn fixes the top of the range, so even at peak demand the
registration burn is held at or below it.

## Default and Setting

The documentation lists a default of 100 TAO and marks the parameter as owner-settable, so a subnet
owner can adjust the ceiling for their subnet. The value in force is per-subnet chain state and can
differ from the default, so the real ceiling should be read for the subnet in question.

## Subnet Context

Max burn is one of the subnet hyperparameters, the on-chain state variables that configure a single
subnet, for example netuid 1. Because each subnet carries its own hyperparameters, the registration
burn ceiling is defined per subnet and applies only to registration on that subnet.

## Development Stage Context

The
[Introduction to Bittensor](https://docs.learnbittensor.org/learn/introduction#subnet-development)
describes subnet development as moving from localnet to testnet and then mainnet. For max burn, that
sequence changes how readers should interpret registration-cost ceiling examples.

In localnet, max burn hyperparameters can be tested in an isolated environment. Localnet burn
ceilings do not represent production registration costs.

On testnet, registration burn rules can be exercised in a shared non-production network. Testnet max
burn values are separate from mainnet subnet state.

On mainnet, max burn is a live per-subnet hyperparameter on production subnets. Observed
registration ceilings depend on the selected subnet's on-chain hyperparameter state
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

The [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks) reference
separates mainnet, testnet, and localnet. A max-burn example from one environment should not be read
as representing production registration costs in another environment.

## BurnHalfLife Eases the Burn During Quiet Periods

The [Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register) describes the
dynamic registration burn as decaying over time according to `BurnHalfLife` while staying inside the
documented `MinBurn` and `MaxBurn` band. Max burn still names only the ceiling; half-life names how
quickly the live burn can fall when registration demand quiets down.

That decay keeps max burn from acting like a fixed price. During slow periods the burn can drift
downward toward the subnet floor, but the documented upper bound remains the point it cannot exceed
even when registrations surge again
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## BurnIncreaseMult Steps the Burn After Each Registration

Registration vocabulary also states that each successful registration can increase the dynamic burn
through `BurnIncreaseMult`. That multiplier responds to demand inside the same min/max band: more
registrations push the live burn upward, while half-life decay pulls it back down when activity
slows.

Max burn therefore caps how far that upward stepping can climb. A subnet owner can raise the ceiling
for their subnet, for example netuid 1, but the multiplier still operates only inside the documented
hyperparameter bounds for that subnet.

## Full Subnets Use Displacement Instead of Rejection

[Mining](https://docs.learnbittensor.org/miners#miner-registration) documentation notes that when a
subnet is already full, registration can displace the lowest-ranked existing participant through
immunity and deregistration rules rather than simply failing. Max burn still bounds the TAO burn for
that entry attempt, but it does not by itself decide which UID slot changes hands.

That keeps economic bounds separate from slot competition. Max burn limits how expensive the attempt
can become; displacement rules decide whether a new registrant can take a slot from an existing
neuron when the subnet has no empty positions.

## Distinction from BurnHalfLife

Max burn caps the dynamic registration burn on an existing subnet. BurnHalfLife names how quickly
that live burn can decay toward the subnet floor when registration demand quiets down
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#burnhalflife),
[Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

- **Max burn** — ceiling on the registration burn band.
- **BurnHalfLife** — decay rate pulling the live burn down during quiet periods.

## Distinction from BurnIncreaseMult

BurnIncreaseMult raises the live registration burn after each successful registration inside the
documented min/max band. Max burn names the upper bound that upward stepping cannot exceed
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#burnincreasemult),
[Subnet Hyperparameters: MaxBurn](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#maxburn)).

Demand spikes can push the live burn upward through the multiplier, but the documented ceiling still
limits how far that climb can go on the selected netuid
([Mining: Miner registration](https://docs.learnbittensor.org/miners#miner-registration)).

Readers should keep the static ceiling separate from the demand-driven live burn. Max burn sets the
upper bound; half-life and increase-multiplier hyperparameters describe how the observed
registration price moves inside the documented band
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

Max burn is owner-settable per subnet alongside min burn, so the ceiling should be read from the
relevant netuid hyperparameter table rather than treated as one network-wide constant
([Understanding Subnets](https://docs.learnbittensor.org/subnets/understanding-subnets)).

## Distinction from Min Burn

Max burn and min burn set the upper and lower bounds of the same dynamic registration burn band on a
subnet. Registration cost moves between those limits as demand changes
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Mining: Miner registration](https://docs.learnbittensor.org/miners#miner-registration)).

- **Max burn** — ceiling the registration burn cannot exceed.
- **Min burn** — floor the registration burn cannot fall below.

## Distinction from Burn Cost

Max burn should not be confused with burn cost. Burn cost is the TAO amount recycled when creating a
new subnet, while max burn bounds the separate dynamic burn paid to register a neuron on a subnet
that already exists
([Glossary: Burn Cost](https://docs.learnbittensor.org/resources/glossary#burn-cost),
[Create a Subnet: Burn cost](https://docs.learnbittensor.org/subnets/create-a-subnet#burn-cost),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#maxburn)).

The two costs sit at different points in the subnet lifecycle. Burn cost is paid once to bring a
subnet into existence; max burn bounds the recurring, demand-driven price of joining an
already-created subnet as a miner or validator.

- **Burn cost** — recycled TAO required to create a new subnet market.
- **Max burn** — ceiling on dynamic neuron registration burn on an existing subnet.

## Chain Reads for netuid 1

Readers can verify live hyperparameter values for the documented example netuid with
`btcli subnet hyperparameters --netuid 1 --network finney`
([Subnet Hyperparameters: View hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#view-hyperparameters)).

That read path keeps live hyperparameter claims tied to a parseable netuid.

## Per-Subnet Live Value Boundary

Max burn is per-subnet chain state. The registration-burn ceiling configured on one netuid caps how
high the dynamic charge can rise on that subnet, but the live ceiling can differ from reference
defaults and from the ceiling on another netuid
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

This article's infobox uses netuid 1 as an example label when discussing registration demand on
Finney mainnet. That example anchors discussion; it does not prove identical ceilings everywhere
([Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)).

Max burn governs registration pricing ceilings; Yuma Consensus still settles validator weights into
emission shares each tempo ([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

- **Max burn** — ceiling the dynamic registration burn cannot exceed on a subnet.
- **Per-subnet boundary** — live value is chain state on one netuid.

## Distinction from Yuma Consensus

Max burn is a per-subnet hyperparameter that sets the ceiling of the dynamic registration burn band.
Yuma Consensus is the separate tempo process that converts validator weight submissions into
emission shares each epoch
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

- **Max burn** — ceiling the registration burn cannot exceed.
- **Yuma Consensus** — mechanism that turns validator weights into emission shares.

## Reader Boundary

This page defines the concept at a high level. It does not report the max burn value set on any
particular subnet or the current registration cost. Those are live chain state and should be checked
for the relevant netuid. The default of 100 TAO is the documented value, and the parameter is
owner-settable per subnet.

## Further Reading

- [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
- [Register](https://docs.learnbittensor.org/resources/glossary#register)
- [Miner registration](https://docs.learnbittensor.org/miners#miner-registration)
- [Glossary: Burn Cost](https://docs.learnbittensor.org/resources/glossary#burn-cost)
