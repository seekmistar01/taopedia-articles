---
title: "Start Call"
summary:
  "How a start call activates a newly created Bittensor subnet so it begins its first emission
  period."
category: "Subnets"
tags: ["Subnets", "Tokenomics"]
infoboxTitle: Start Call
infoboxCaption: Activation that begins subnet emissions.
infoboxRows:
  - label: Context
    value: Bittensor subnets
  - label: Example
    value: netuid 1
  - label: Marks
    value: First emission
  - label: Scope
    value: Concept only
---

# Start Call

A start call is the activation step that turns on a newly created subnet's emissions. Official
documentation describes activating a subnet to begin its first emission period, after which the
subnet starts distributing alpha to its participants.

## Before and After Activation

Creating a subnet and emitting on it are separate milestones. A freshly registered subnet exists on
the network but does not yet emit. The start call begins its first emission period, and from that
point the documentation notes that the subnet creator starts accumulating emissions and the subnet's
token economy shows a non-zero emission amount.

## Place in the Subnet Lifecycle

The start call separates subnet creation from the start of emissions. That gap lets a subnet be
registered and prepared before it begins distributing alpha, rather than emitting from the instant
it is created. Once activated, accumulated rewards are distributed to participants each tempo
through consensus.

## Development Stage Context

The
[Introduction to Bittensor](https://docs.learnbittensor.org/learn/introduction#subnet-development)
describes subnet development as moving from localnet to testnet and then mainnet. For a start call,
that sequence changes which network a reader should assume when asking whether a subnet is emitting.

In localnet, subnet registration and activation can be tested in an isolated environment. Localnet
activation state does not represent production emission timing on live subnets.

On testnet, a start call can be issued on a shared non-production network. Testnet activation and
emission history are separate from mainnet subnet state
([Create a Subnet](https://docs.learnbittensor.org/subnets/create-a-subnet)).

On mainnet, a start call concerns live production subnet activation and the first emission period on
the selected [netuid](https://docs.learnbittensor.org/resources/glossary#netuid).

The [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks) reference
separates mainnet, testnet, and localnet. An activation example from one environment should not be
read as describing emission status on another network.

## Subnet Registration Precedes Activation

[Create a Subnet](https://docs.learnbittensor.org/subnets/create-a-subnet) documentation treats
subnet registration and the start call as separate steps. A participant first registers the subnet
on [Subtensor](https://docs.learnbittensor.org/resources/glossary#subtensor), paying the documented
registration cost in [TAO](https://docs.learnbittensor.org/resources/glossary#tao-tau), and only
later issues the start call that begins emissions.

That ordering keeps economic setup separate from emission timing. Registration establishes the
subnet's identity and owner on chain, while the start call is the later switch that turns on the
first [emission](https://docs.learnbittensor.org/resources/glossary#emission) period for that
[netuid](https://docs.learnbittensor.org/resources/glossary#netuid).

## Alpha Issuance Begins at the Start Call

[Understanding Subnets](https://docs.learnbittensor.org/subnets/understanding-subnets) explains that
each subnet issues its own dynamic token, referred to in the abstract as that subnet's
[alpha](https://docs.learnbittensor.org/resources/glossary#protocol-alpha). The start call marks
when that per-subnet issuance path becomes active rather than when the subnet record is first
created.

Before activation, the subnet can exist without distributing alpha to participants. After the start
call, creation guidance notes that the subnet creator begins accumulating emissions and the subnet's
token economy shows a non-zero emission amount tied to ongoing subnet activity.

## Tempo Settlement Follows Activation

Once emissions are running, [Emission](https://docs.learnbittensor.org/learn/emissions)
documentation describes rewards accumulating during each subnet
[tempo](https://docs.learnbittensor.org/resources/glossary#tempo) and being distributed through
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus) at the interval boundary. The
start call therefore begins both alpha issuance and the tempo rhythm that groups participant
payouts.

That keeps activation vocabulary separate from creation vocabulary. Registration answers whether the
subnet exists on chain; the start call answers when its first tempo-sized emission and distribution
cycle can begin for miners, validators, and stakers who register afterward.

## Distinction from Subnet Is Active

Start call is the action that begins a subnet's emissions, while subnet is active is the resulting
state that reports whether those emissions have started. One is the trigger; the other is the status
it produces ([Emission](https://docs.learnbittensor.org/learn/emissions),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

- **Start call** — the operation that switches a subnet's emissions on.
- **Subnet is active** — the flag that reports emissions have started.

## Distinction from Immunity Period

A start call and an immunity period name an activation step and a protection window. The start call
is the operation that turns on a subnet's first emission period, while the immunity period is the
grace window during which a newly registered neuron is shielded from deregistration so it has time
to establish performance
([Create a Subnet](https://docs.learnbittensor.org/subnets/create-a-subnet),
[Glossary: Immunity Period](https://docs.learnbittensor.org/resources/glossary#immunity-period)).

- **Start call** — the activation step that begins a subnet's emissions.
- **Immunity period** — the grace window protecting a new neuron from early deregistration.

## Distinction from Recycle

A start call and recycle name an activation step and a TAO-consuming mechanism. The start call
begins a subnet's emissions once it is registered, while recycle describes TAO that is removed from
a holder's balance and returned to the supply for re-issuance, such as the TAO consumed by
registration ([Create a Subnet](https://docs.learnbittensor.org/subnets/create-a-subnet),
[Glossary: Recycle](https://docs.learnbittensor.org/resources/glossary#recycle)).

- **Start call** — the step that switches a registered subnet's emissions on.
- **Recycle** — TAO returned to the supply for re-issuance, as with the registration cost.

## Distinction from Participation

Start call is the activation step that turns on a subnet's first emission period. Participation
names miner and validator activity on that subnet once emissions are running. Before the start call
there is no emission flow for that work to draw from
([Create a Subnet](https://docs.learnbittensor.org/subnets/create-a-subnet),
[Understanding Subnets](https://docs.learnbittensor.org/subnets/understanding-subnets)).

- **Start call** — the activation that begins subnet emissions.
- **Participation** — miner and validator activity that becomes economically live once emissions
  run.

## Distinction from Subnet Creator

A subnet creator is the participant who registers and owns a subnet. The start call is the
activation step that creator uses to begin the subnet's first emission period, so a subnet can exist
on chain before its creator issues the start call that makes it economically live
([Glossary: Subnet Creator](https://docs.learnbittensor.org/resources/glossary#subnet-creator),
[Create a Subnet](https://docs.learnbittensor.org/subnets/create-a-subnet)).

- **Subnet creator** — the role that owns and activates a subnet.
- **Start call** — the activation event that turns on emissions.

## Distinction from Yuma Consensus

Start call names the activation step that turns on a newly created subnet's first emission period.
Yuma Consensus is the separate tempo process that converts validator weight submissions into
emission shares after the subnet is active
([Create a Subnet](https://docs.learnbittensor.org/subnets/create-a-subnet),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

- **Start call** — activation that begins a subnet's first emission period.
- **Yuma Consensus** — mechanism that turns validator weights into emission shares.

## Reader Boundary

This page defines the concept at a high level. It does not report whether a particular subnet has
been activated, when its start call happened, or its current emissions. Those are live chain state
and should be checked for the relevant netuid, for example netuid 1. The article describes the role
of the activation, not a procedure to perform it.

## Further Reading

- [Create a Subnet](https://docs.learnbittensor.org/subnets/create-a-subnet)
- [Understanding Subnets](https://docs.learnbittensor.org/subnets/understanding-subnets)
- [Glossary: Subnet Creator](https://docs.learnbittensor.org/resources/glossary#subnet-creator)
- [Emission](https://docs.learnbittensor.org/learn/emissions)
